// Optional captions for the Photo Gallery page (gallery.html) —
// "Infinite Fold".
//
// Photos themselves are NOT edited here. Drop a file into images/gallery/,
// run scripts/build-gallery-manifest.ps1 (or double-click
// scripts/update-gallery.bat), and it shows up automatically — no data-file
// editing required. That script regenerates js/gallery-manifest.js, which
// gallery.js reads to know what's in the folder.
//
// To give a photo a title/place/tech line, add an entry here keyed by its
// filename exactly as it appears in images/gallery/. Uncaptioned photos
// still show — they just display with no caption/title at all.
window.GALLERY_CAPTIONS = {};
