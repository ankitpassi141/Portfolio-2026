// Editable content for the private /raw companion to the Assessment
// Generator case study. Not linked from anywhere on the site — reachable
// only by knowing the URL (study/automated-test/raw/).
//
// This is meant to hold the more candid version of the write-up: process
// notes, dead ends, internal detail that doesn't belong in the polished
// public case study. Free-form — add, remove, or reorder entries in
// `sections` however you want; each is just a heading + one or more
// paragraphs.
window.CASE_STUDY_RAW = {
  pageTitle: "Assessment Generator (raw) — Ankit Passi",
  backHref: "../../automated-test.html",
  backLabel: "← Back to case study",

  eyebrow: "automated test generator",
  title: "Assessment Generator — Raw Notes",

  // Replace with the real notes. One entry per section; `paras` is an
  // array of strings, one per paragraph.
  sections: [
    {
      h3: "Add your raw notes here",
      paras: [
        "This page is a private companion to the polished case study — only reachable if you know this URL, not linked from anywhere on the site. Write whatever didn't make it into the public write-up: rejected directions, the actual messy timeline, internal politics, things you'd say differently now."
      ]
    }
  ]
};
